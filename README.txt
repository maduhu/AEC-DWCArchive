Created using the following command-line:

   # mysqldump --databases pbi_locality --default-character-set=latin1 --ignore-table=pbi_locality.UUser > pbi_localitylatin1.sql

 #run php darwinCore.php
 # run occurrences.sql for each project separatly
 # add occurrences.sql to each archive
 # zip archive
 # check paths in RSS and .eml files
 # upload to server for idigbio